{
  "env": {
    "description": "This workflow (.github/workflows/publish-release-to-pypi.yaml) was generated from ci/gen.py"
  },
  "jobs": {
    "publish": {
      "environment": {
        "name": "pypi-release",
        "url": "https://pypi.org/p/humphrey"
      },
      "name": "Publish the release package to PyPI",
      "permissions": {
        "id-token": "write"
      },
      "runs-on": "ubuntu-latest",
      "steps": [
        {
          "name": "Check out repository",
          "uses": "actions/checkout@v5"
        },
        {
          "name": "Publish the package to PyPI",
          "run": "sh ci/publish.sh"
        }
      ]
    }
  },
  "name": "Publish the release package to PyPI",
  "on": {
    "release": {
      "types": [
        "published"
      ]
    }
  }
}